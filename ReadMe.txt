	//Download and install libraries with command
> npm install

	//For development:

	//Run development server and load code. In browser open 'localhost:9011'
> npm run dev
	
	//For production:
	
	//Run bild. It create 'dist' folder and creating index.html, and generating minified 'bundle.js' files.
> npm run build
	
	//Run server that’s reading code from generated 'dist' folder.
> npm start

	//Run tests
>npm test

	//Run tests with summary table
>npm run test:cov

	//Run tests in watch mode
>npm run test:watch
